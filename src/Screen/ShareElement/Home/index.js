import React, {useEffect} from 'react';
import {Button, Image, Text, View} from 'react-native';

// Styles
import styles from './styles';

const HomeShared = ({navigation}) => {
  return (
    <View>
      <Text>HomeShared</Text>
    </View>
  );
};

export default HomeShared;
