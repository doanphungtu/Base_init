import React, {useEffect} from 'react';
import {Button, Image, Text, View} from 'react-native';

// Styles
import styles from './styles';

const DetailShared = ({navigation}) => {
  return (
    <View>
      <Text>DetailShared</Text>
    </View>
  );
};

export default DetailShared;
